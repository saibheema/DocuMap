# ──────────────────────────────────────────────────────────────
# DocuMap API — environment variables
# Copy this file to .env and fill in the real values.
# Never commit .env to git.
# ──────────────────────────────────────────────────────────────

# Google / Gemini AI
GEMINI_API_KEY=your_gemini_api_key_here
# (Some SDK versions read this name instead)
# GOOGLE_AI_API_KEY=your_gemini_api_key_here

# Firebase Admin SDK
# Option 1 — path to a service-account JSON key file:
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Option 2 — inline JSON (base64-encoded) for Cloud Run / CI:
# FIREBASE_SERVICE_ACCOUNT_BASE64=<base64 of service-account.json>

# Firebase project ID (must match your Firebase / GCP project)
FIREBASE_PROJECT_ID=cs-hc-8f0222acc86c43eca440f9a4

# Server
PORT=4000
